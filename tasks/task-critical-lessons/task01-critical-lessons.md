# Task 01: Project Initialization & Environment Setup - Critical Lessons

*   **Task Master Setup:** Established the `.taskmaster` directory structure and workflow early to enforce documentation and process adherence from the start.
*   **Environment Pinning:** Created `environment.yml` but noted that specific versions (Python, CUDA, PyTorch) need confirmation based on VILA/NVILA requirements (Task 02).
*   **Git Initialization:** `.gitignore` created, but `git init` must be run manually by the user. 